# 📚 Git 使用指南

> 財報分析知識庫的版本控制快速參考

---

## 🎯 基本 Git 工作流程

### 每次修改後的標準流程

```bash
# 1. 查看修改狀態
git status

# 2. 查看具體修改內容
git diff

# 3. 添加修改到暫存區
git add .                    # 添加所有修改
git add README.md            # 添加特定文件

# 4. 提交修改
git commit -m "更新說明"

# 5. 查看提交歷史
git log --oneline
```

---

## 📝 提交訊息建議格式

### 基本格式
```
<類型>: <簡短描述>

[可選的詳細說明]
```

### 常用類型

| 類型 | 說明 | 範例 |
|------|------|------|
| ✨ **feat** | 新增功能 | `feat: 新增 META 完整估值分析` |
| 📝 **docs** | 文檔更新 | `docs: 更新 README 使用說明` |
| 🐛 **fix** | 修正錯誤 | `fix: 修正 PINS 估值計算錯誤` |
| 💼 **portfolio** | 投資組合更新 | `portfolio: 更新 11月持倉數據` |
| 📊 **analysis** | 分析報告 | `analysis: 完成 GOOGL Q3 分析` |
| 🔧 **chore** | 雜項維護 | `chore: 清理舊檔案` |
| ♻️ **refactor** | 重構 | `refactor: 重組 templates 結構` |

### 範例提交訊息

**簡短版**：
```bash
git commit -m "feat: 新增 META Q3 完整估值分析"
```

**詳細版**：
```bash
git commit -m "portfolio: 更新 2025-11 月度持倉數據

## 變更內容
- 新增 META 持倉 10股 @$570
- 更新 PINS 市值
- 計算月度報酬率 -5.2%

## 績效摘要
- 總報酬：-8.5%
- vs S&P 500: -12.3%
"
```

---

## 🔄 常用 Git 操作

### 查看歷史

```bash
# 查看提交歷史（簡短）
git log --oneline

# 查看提交歷史（詳細）
git log --stat

# 查看最近 5 筆提交
git log -5

# 查看特定文件的歷史
git log -- portfolio/2025/holdings.md
```

### 撤銷操作

```bash
# 撤銷尚未暫存的修改
git checkout -- README.md

# 取消暫存（但保留修改）
git reset HEAD README.md

# 修改最後一次提交訊息
git commit --amend

# 回到上一個提交（危險！）
git reset --hard HEAD~1
```

### 比較差異

```bash
# 查看工作區 vs 暫存區
git diff

# 查看暫存區 vs 最後提交
git diff --staged

# 比較兩次提交
git diff commit1 commit2

# 查看特定文件差異
git diff portfolio/2025/holdings.md
```

---

## 🌿 分支管理（進階）

### 基本分支操作

```bash
# 查看所有分支
git branch

# 創建新分支
git branch feature/new-template

# 切換分支
git checkout feature/new-template

# 創建並切換（快捷）
git checkout -b feature/new-template

# 合併分支
git merge feature/new-template

# 刪除分支
git branch -d feature/new-template
```

### 建議的分支策略

```
main (主分支)
├── analysis/pins-q4          # 個股分析
├── feature/automation-tools   # 新功能開發
├── docs/update-readme         # 文檔更新
└── portfolio/2025-q4          # 投資組合更新
```

---

## 🔄 實際使用場景

### 場景 1：完成一份新分析報告

```bash
# 1. 創建分析
vim analysis/2025Q4/META.md

# 2. 查看變更
git status
git diff

# 3. 提交
git add analysis/2025Q4/META.md
git commit -m "analysis: 完成 META Q4 財報分析

- 4種估值方法
- 目標價 $650-680
- 評級：強力買入 ⭐⭐⭐⭐⭐
"

# 4. 更新索引
vim INDEX.md
git add INDEX.md
git commit -m "docs: 更新 INDEX 新增 META 分析"
```

### 場景 2：每月更新投資組合

```bash
# 1. 更新持倉數據
vim portfolio/2025/holdings.md

# 2. 更新績效
vim portfolio/2025/performance.md

# 3. 一次提交
git add portfolio/
git commit -m "portfolio: 2025-12 月度更新

## 交易記錄
- 買入 GOOGL 50股 @$155

## 績效
- 月度報酬：+3.2%
- 累積報酬：-2.1%
- vs S&P 500: +1.5%
"
```

### 場景 3：定期備份與檢查點

```bash
# 每週五備份
git add .
git commit -m "chore: 週度備份 2025-11-08"

# 季度檢查點
git add .
git commit -m "chore: 2025 Q4 季度檢查點

完成項目：
- 5份完整分析報告
- 10個觀察清單標的
- 組合報酬 +8.5%
"
```

---

## 🚀 進階技巧

### 標籤（Tags）

用於標記重要的里程碑：

```bash
# 創建標籤
git tag -a v1.0 -m "知識庫 v1.0 - 初始版本"

# 查看所有標籤
git tag

# 查看特定標籤
git show v1.0

# 標記年度總結
git tag -a 2025-annual -m "2025年度投資總結"
```

### 查看文件歷史變更

```bash
# 查看文件的所有變更
git log -p portfolio/2025/holdings.md

# 查看誰修改了哪一行
git blame portfolio/2025/holdings.md

# 查找特定內容的歷史
git log -S "PINS" --source --all
```

### Git 別名（快捷命令）

在 `~/.gitconfig` 添加：

```ini
[alias]
    st = status
    ci = commit
    co = checkout
    br = branch
    lg = log --oneline --graph --decorate
    last = log -1 HEAD
    unstage = reset HEAD --
```

使用：
```bash
git st           # = git status
git lg           # 美化的 log
git last         # 查看最後一次提交
```

---

## 📦 備份與同步

### 連接遠端倉庫（GitHub/GitLab）

```bash
# 連接遠端
git remote add origin https://github.com/yourusername/financial-report.git

# 推送到遠端
git push -u origin main

# 之後的推送
git push

# 從遠端拉取
git pull
```

### 本地備份

```bash
# 壓縮整個倉庫
tar -czf financial-report-backup-$(date +%Y%m%d).tar.gz .

# 或複製到另一個位置
cp -r /path/to/financial-report /backup/location/
```

---

## ⚠️ 注意事項

### 不要提交的文件

已在 `.gitignore` 設定：
- ✅ 敏感資訊（API keys, 密碼）
- ✅ 大型數據文件
- ✅ 編譯文件
- ✅ 系統臨時文件

### 最佳實踐

1. **頻繁提交**：每完成一個小任務就提交
2. **清晰訊息**：寫清楚為什麼修改，不只是改了什麼
3. **原子性提交**：一次提交只做一件事
4. **定期備份**：每週推送到遠端或外部硬碟
5. **分支開發**：大型修改使用分支

---

## 🆘 緊急救援

### 不小心刪除了文件

```bash
# 恢復單個文件
git checkout HEAD -- deleted-file.md

# 恢復所有刪除
git checkout HEAD -- .
```

### 提交錯了內容

```bash
# 撤銷最後一次提交（保留修改）
git reset --soft HEAD~1

# 完全撤銷最後一次提交（危險！）
git reset --hard HEAD~1
```

### 查看某個時間點的版本

```bash
# 查看歷史提交
git log --oneline

# 切換到特定提交
git checkout <commit-hash>

# 回到最新版本
git checkout main
```

---

## 📚 更多學習資源

- 📖 [Git 官方文檔](https://git-scm.com/doc)
- 🎓 [Learn Git Branching](https://learngitbranching.js.org/)
- 📺 [Git 教學影片](https://www.youtube.com/watch?v=USjZcfj8yxE)

---

**建立日期**：2025-11-05
**維護者**：您的名字

---

*版本控制是保護工作成果的最佳方式。養成良好的提交習慣，未來的自己會感謝你！* 🚀
